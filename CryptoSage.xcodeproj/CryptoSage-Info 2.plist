<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Human‑readable display name shown on the Home Screen -->
	<key>CFBundleDisplayName</key>
	<string>CryptoSage</string>

	<!-- SwiftUI lifecycle: declare the scene manifest (no storyboard) -->
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneConfigurationName</key>
					<string>Default Configuration</string>
					<key>UISceneDelegateClassName</key>
					<string></string>
				</dict>
			</array>
		</dict>
	</dict>

	<!-- Privacy: Photos access for attaching images in AI chat -->
	<key>NSPhotoLibraryUsageDescription</key>
	<string>Allow CryptoSage to access your photos to attach images in chat.</string>

	<!-- App Transport Security: keep the app strict, but allow mixed content inside WKWebView if needed -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<false/>
		<key>NSAllowsArbitraryLoadsInWebContent</key>
		<true/>
	</dict>

	<!-- Optional: declare that we don't use non-exempt encryption outside standard TLS (helps App Store review) -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
</dict>
</plist>
